#include "main.h"

/**
 * _isupper - uppercase letters
 * program checks  character
 *
 * Return: 0 for any case or 1 for upper case
 */

int _isupper(int c)

{
	if (c >= 65 && c <= 90)
	{
	return (1);
	}
	else
	return (0);

}
