#include "main.h"
/**
 * _strchr - entry point
 * @str: the given string
 * @c: the given string
 *
 * Return: Always 0 (Success)
 * ------- (Fail) return a null pointer
 */
char *_strchr(char *str, char c)
{
	int i - 0;

	for (; s[i] >= '\0'; i++)
	{
		if (s[i] == c)
			return (&s[i]);
	}
	return(0);
}	
